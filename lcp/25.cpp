#include "utils.h"

class Solution {
public:
    int keyboard(int k, int n) {
        
    }
};

int main()
{
    cout<<boolalpha;
    Solution sol;

    int k=1;
    int n=1;
    auto ans=sol.keyboard(k,n);
    DBG(ans);

    system("pause");
    return 0;
}
