#include "utils.h"

class Solution {
public:
    int countArrangement(int n) {
        int dp[n][1<<n];
        REP(i,n){
            dp[]
        }

    }
};

int main()
{
    cout<<boolalpha;
    Solution sol;

    int n=2;
    auto ans=sol.countArrangement(n);
    DBG(ans);

    system("pause");
    return 0;
}
